import logo from '../../img/logo.png'

const Header = () => {
    return (
        <header className='container'>
            <img
                src={logo}
                alt=''
            />
        </header>
    )
}

export default Header
